name: Keep Alive API

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request to keep API alive
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://semac-backend.onrender.com/palestra
